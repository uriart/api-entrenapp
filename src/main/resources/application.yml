#
# JDBC Properties
#
spring:
  datasource:
    url: jdbc:postgres:as1w4HfnH0UB7AFWN36S@containers-us-west-90.railway.app:7419/railway
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-r071w8pk.eu.auth0.com/

auth0:
  audience: https://users-api-test2.herokuapp.com/

